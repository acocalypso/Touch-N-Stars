name: Build Bundle & Publish Beta Release

on:
  workflow_dispatch: {}

jobs:
  build-and-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Build web bundle
        run: npm run build:native

      - name: Package dist assets
        run: |
          cd dist
          zip -r ../dist.zip .

      - name: Read package version
        id: pkg
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build beta tag
        id: beta
        run: |
          TAG="v${{ steps.pkg.outputs.version }}-beta.${{ github.run_number }}.${{ github.run_attempt }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Ensure tag does not already exist
        run: |
          if git rev-parse "${{ steps.beta.outputs.tag }}" >/dev/null 2>&1; then
            echo "Tag ${{ steps.beta.outputs.tag }} already exists."
            exit 1
          fi

      - name: Create and push tag
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"
          git tag -a "${{ steps.beta.outputs.tag }}" -m "Beta release ${{ steps.beta.outputs.tag }}"
          git push origin "${{ steps.beta.outputs.tag }}"

      - name: Upload dist.zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-beta
          path: dist.zip

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.beta.outputs.tag }}
          name: ${{ steps.beta.outputs.tag }}
          prerelease: true
          make_latest: false
          files: |
            dist.zip
            CHANGELOG.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
