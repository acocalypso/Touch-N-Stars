# Celestia Atlas Context7 log

| Library       | Locked version | Context7 ID                  | Question                                                                                  | Finding                                                                                                                                                | Applied decision                                                                                                          | Confidence                                                                   |
| ------------- | -------------: | ---------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| Vue           |         3.5.39 | `/vuejs/vue`                 | How should external renderer resources be cleaned up?                                     | External integrations must be torn down with the component lifecycle.                                                                                  | The wrapper will cancel animation work, disconnect observers/listeners, and call `destroy()` on unmount.                  | Medium; returned example was not version-specific.                           |
| Vite          |          8.1.3 | `/vitejs/vite/v8.0.10`       | How should package assets survive builds and non-root base paths?                         | Static `new URL('./asset', import.meta.url)` references are transformed for production; `base` rewrites built asset paths.                             | Engine-owned assets will be module-relative, not root-relative public URLs.                                               | High                                                                         |
| Vite          |          8.1.3 | `/vitejs/vite/v8.0.10`       | Can the production viewer default be selected with an explicit rollback env flag?         | `VITE_` variables are strings and statically replaced at build time, enabling tree-shaking of conditional imports.                                     | Normal builds import Celestia Atlas; only `VITE_STELLARIUM_ROLLBACK=true` imports the legacy viewer.                      | High                                                                         |
| Capacitor App |          8.1.0 | `/ionic-team/capacitor-docs` | How is `appStateChange` registered and removed?                                           | `addListener` returns a `PluginListenerHandle`; its `remove()` method removes that listener.                                                           | Retain the viewer lifecycle listener handle and remove that handle instead of calling global `removeAllListeners()`.      | High                                                                         |
| Pinia         |          3.0.4 | `/vuejs/pinia`               | How are component subscriptions disposed?                                                 | `$subscribe()` returns an unsubscribe function and component-scoped subscriptions auto-dispose unless detached.                                        | Keep renderer objects outside Pinia; explicitly dispose any adapter subscriptions.                                        | Medium; Context7 surfaced v4 docs, so actual v3 usage must be cross-checked. |
| Vue           |         3.5.39 | `/websites/vuejs_guide`      | Should an expensive async viewer use `v-if` or `v-show`?                                  | `v-if` is lazy and conditionally mounts; `v-show` always mounts and only changes CSS display.                                                          | Gate the first Atlas open with a latched `v-if`, then retain the warm instance with `v-show`.                             | High                                                                         |
| Vite          |          8.1.4 | `/vitejs/vite/v8.0.10`       | How should the lazy Atlas graph be split without the catch-all vendor group absorbing it? | Higher-priority code-splitting groups are evaluated before lower-priority catch-all groups; emitted module preloads still require artifact inspection. | Put catalogue, bright-sky and engine groups before `vendor`, then verify `dist/index.html` and the runtime request graph. | High                                                                         |

Context7 did not return an exact `vue-tsc`/Vue Language Tools package entry for
the TypeScript 7 compatibility failure. The official Vue Language Tools/npm
metadata says `vue-tsc` wraps `tsc` and declares TypeScript 5 or newer, but
`vue-tsc 3.3.7` resolves `typescript/lib/tsc`, which TypeScript 7.0.2 no longer
exports in this installation. The repository's previously working TypeScript
6.0.3 pin was restored and the complete typecheck passed.
